enum StdSuit {
    Hearts = 0,
    Spades,
    Diamonds,
    Clubs,
    Unsuited
}   // enum StdSuit

enum StdFace {
    Ace = 0,
    Two,
    Three,
    Four,
    Five,
    Six,
    Seven,
    Eight,
    Nine,
    Ten,
    Jack,
    Queen,
    King,
    Joker,
}   // enum StdFace

enum DeckType {
    Poker,
    Pinochle,
    Euchre,
    //% block="Custom (not for use in Blocks)"
    Custom,
}   // enum DeckType

enum CardSpriteSize {
    Size8x8 = 0,
    Size8x16,
    Size16x16,
    Size16x32,
    Size32x32,
}   // enum CardSpriteSize
